# This module runs on Linux (Jazzer/Maven). Force LF so shell scripts and Python
# stay executable regardless of the checkout platform.
*.sh   text eol=lf
*.py   text eol=lf
*.java text eol=lf
*.md   text eol=lf
*.xml  text eol=lf
